---
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionAbout from "../components/SectionAbout.astro"
const posts = await Astro.glob("../pages/content/*.md");
// const { frontmatter } = Astro.props;

// console.log(frontmatter);

const list = await Astro.glob('../pages/content/pinpoints.md');
---

<style>
	.content__heading {
		font-size: var(--fs-huge);
		line-height: normal;
	}



	.content__text {
		font-weight: 400;
		font-size: var(--fs-m);
		max-width: 70%;
		margin-top: var(--s1);
		line-height: 1em;
	}
</style>

<BaseLayout>
	<div class="content__flex">
		<p class="festival__date">24-25 June</p>
		<p class="festival__location">Saint-Rémy-de-Provence, France</p>
	</div>
	<p class="content__heading">Grape Festival</p>
	<p class="content__text">
		Grape Festival is not only about wine, do you believe that? Dive into
		the world of grapes like never before at our unique Grape Festival! This
		extraordinary event celebrates the versatility and beauty of grapes,
		transcending beyond the realm of wine.
	</p>
</BaseLayout>
